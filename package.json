{
  "name": "crm-process-builder",
  "version": "1.0.0",
  "description": "CRM Business Process Builder",
  "main": "server.js",
  "scripts": {
    "start": "ts-node server.ts",
    "build": "tsc && tsc -p tsconfig.server.json",
    "check": "tsc --noEmit && tsc -p tsconfig.server.json --noEmit"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.0.0",
    "@types/express": "^4.17.21"
  }
}
